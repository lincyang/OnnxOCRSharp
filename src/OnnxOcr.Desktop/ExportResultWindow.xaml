<Window x:Class="OnnxOcr.Desktop.ExportResultWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="导出完成"
        Width="440"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Background="#F7F8FA">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="已导出 Excel"
                   FontSize="18"
                   FontWeight="SemiBold"
                   Foreground="#1F2A44" />

        <TextBlock x:Name="FileNameText"
                   Grid.Row="1"
                   Margin="0,10,0,4"
                   Foreground="#374151"
                   TextWrapping="Wrap" />

        <TextBlock x:Name="DirectoryText"
                   Grid.Row="2"
                   Foreground="#6B7280"
                   FontSize="12"
                   TextWrapping="Wrap"
                   Margin="0,0,0,20" />

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="打开文件"
                    Width="96"
                    Height="32"
                    Margin="0,0,8,0"
                    Click="OnOpenFileClick" />
            <Button Content="打开所在目录"
                    Width="110"
                    Height="32"
                    Margin="0,0,8,0"
                    Click="OnOpenFolderClick" />
            <Button Content="关闭"
                    Width="72"
                    Height="32"
                    IsDefault="True"
                    IsCancel="True"
                    Click="OnCloseClick" />
        </StackPanel>
    </Grid>
</Window>
